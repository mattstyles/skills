# Agent skills

## Productivity

Product work flows through planning and design to implementation.
The skills presented here define workflows focussed on deliverables.

```
idea -> gorilla -> prd -> plan -> breakdown
gorilla -> plan -> breakdown
```

The first workflow is for new product surface that needs a PRD. The second is for changes that don't need a full PRD — go straight from grilling to a plan.

These workflows create a base for delivering the work.

* **idea** - Quickly record an idea for future development.
  ```
  npx skills@latest add mattstyles/skills/idea
  ```

* **gorilla** - Relentless questioning to gain alignment and explore each arm of the decision tree.
  ```
  npx skills@latest add mattstyles/skills/gorilla
  ```

* **prd** - Create a product requirements document based on the conversation.
  ```
  npx skills@latest add mattstyles/skills/prd
  ```

* **plan** - Converts a PRD or discussion into an actionable plan.
  ```
  npx skills@latest add mattstyles/skills/plan
  ```

* **breakdown** - Break the plan into trackable tickets (default: uses beads).
  ```
  npx skills@latest add mattstyles/skills/breakdown
  ```

## Git

Git helpers.

* **gq** - Quick for branch, commit, pr.
  ```
  npx skills@latest add mattstyles/skills/gq
  ```

* **gbr**
  ```
  npx skills@latest add mattstyles/skills/gbr
  ```

* **gco**
  ```
  npx skills@latest add mattstyles/skills/gco
  ```

* **gpr**
  ```
  npx skills@latest add mattstyles/skills/gpr
  ```

